# Environment Variables Example for IPL Auction Backend

# Server Configuration
PORT=5001
NODE_ENV=development

# CORS Origins (comma-separated URLs for production)
CORS_ORIGINS=http://localhost:3000,http://localhost:5002

# Database Configuration
DATABASE_URL=./auction.db

# Socket.IO Configuration (optional)
SOCKET_CORS_ORIGIN=*