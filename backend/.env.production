# Production Environment Variables for Railway

PORT=$PORT
NODE_ENV=production

# Railway provides automatic CORS handling, but we can specify if needed
CORS_ORIGINS=*

# Database will use SQLite file in Railway storage
DATABASE_URL=./auction.db

# For Railway deployment
SOCKET_CORS_ORIGIN=*